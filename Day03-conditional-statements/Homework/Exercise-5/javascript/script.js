let r = 20;
let s1 = 10;
let s2 = 20;
let s3 = 30;

let shape = prompt("Enter any one operation of the triangle, circle, circumference, perimeter")
areaT = 


switch (shape) {
    case 'triangle':
        console.log(`Area of triangle is ${areaT}`);
        break;
    case 'circle':
        console.log(`Area of circle is ${areaC}`);
        break;
    case 'circumference':
        console.log(`Circumference of a circle is ${circumference}`);
        break;
    case 'perimeter':
        console.log(`Perimeter of a triangle is ${perimeter}`);
        break;
    default:
        console.log(`Invalid choice`);
}
